# TODO

## 今回の作業
- [x] `condition1` ページでも拡張機能を有効化する。理由: HOME'S の別URLでも同じ操作を継続して使えるようにするため。
- [x] ステータス定義を一元化し、表示順を `除外 → 要確認 → 本命 → 比較中` にそろえる。理由: UI 上の選択順と運用上の優先順を一致させるため。

## レビュー
- [x] `manifest.json` の match/permission を見直し、`condition1` を含めても不要なURLに広がっていないことを確認する。
- [x] ステータス選択肢とバッジ表示が新しい順序で一貫していることを確認する。

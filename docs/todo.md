# TODO

## 今回の作業
- [x] `HOME'S condition1` の検索結果 bundle 幅を少し広げ、画像・条件・チェック列が窮屈になりにくいよう調整する。意図: 拡張の UI を足したあとも、元サイトの一覧テーブルを無理なく読める横幅を確保するため。
- [x] manual link で別掲載を束ねるとき、shared comment は新しい 1 件で上書きせず、既存コメントも重複なく連結して残す。意図: 後からリンクした掲載のメモだけが残り、先に書いていた判断材料を失わないようにするため。
- [x] `紐づけ一覧` / `もしかして` の候補表示を `listingId` 羅列からリンクグループ単位の折りたたみ UI へ切り替える。意図: 候補が多いときでも shared state/comment を保ったまま必要なグループだけ確認しやすくするため。
- [x] `紐づき中` / `リンク` / `もしかして` の各候補で、1 件見つかったらその `effectiveGroupId` に属する掲載をまとめて展開表示する。意図: 候補判定の入口が 1 件でも、実際に連動する掲載全体を見落とさないようにするため。
- [x] 共有ステータスとまとめコメントをグループヘッダへ寄せ、個別行からは shared comment を外す。意図: グループ共通情報と物件ごとの差分メモの責務を分けて一覧を読みやすくするため。
- [x] grouped UI の主要フックを smoke に追加し、変更前後で `node ./tests/smoke.js` を実行する。意図: 折りたたみ化で link 操作と候補 UI の主要構造を落とさないため。
- [x] `URLでリンク` のサジェスト一覧も、単発カードではなく link 候補と同じ group 折りたたみ表示へ揃える。意図: 候補の確認導線を 1 つに揃え、どの入口でも同じ読み方で比較できるようにするため。
- [x] group 化した `紐づき中` / `リンク` / `もしかして` / `URLサジェスト` の操作ボタンを、各メンバー行ではなく group ヘッダ側へ寄せる。意図: group 全体に効く操作だと明示し、行ごとのボタン重複をなくすため。
- [x] ただし `解除` は 1 件単位で必要なため、`紐づき中` だけは各メンバー行に `解除` を残し、`リンク` 系だけ group ヘッダ右側へ寄せる。意図: 操作単位と UI の位置を一致させるため。
- [x] `紐づき中` には各行の `解除` を残したまま、group ヘッダ側にも group 全体解除を置けるようにする。意図: 個別解除と一括解除の両方を同じ grouped UI で使えるようにするため。
- [x] `athome` 東京一覧を `SITE_CONFIGS` の新規 adapter で追加し、既存 3 サイトと同じ保存・フィルタ・リンク基盤に乗せる。意図: 今後のサイト追加でも既存の site adapter 契約を再利用できるようにするため。
- [x] `athome` のページャから `次へ` を解決し、後続ページの building block を `Page N` 区切り付きで現在ページへ連結できるようにする。意図: 実ページでも既存サイトと同じ複数ページ連結体験を揃えるため。
- [x] `athome` の sample fixture、manifest、smoke、storage doc を同じ change で更新する。意図: 実装と再利用可能な検証・運用ドキュメントを同じ前提へ揃えるため。
- [x] 新しいサイト追加手順を `docs/site_addition_workflow_v1.0.md` に正本化する。意図: 次回以降のサイト追加時に判断順と確認順を迷わないようにするため。
- [x] SUUMO の `紐づけ一覧` / `もしかして` が HOME'S と同じ幅感で安定表示されるよう、table 内 panel の幅制約を補正する。意図: サイトごとの DOM 差でリンク一覧の見え方が崩れないようにするため。
- [x] snapshot HTML を基準に、最小の smoke と差分確認で今回の幅調整が既存リンク UI を壊していないことを確認する。意図: 見た目修正のつもりで一覧操作や他サイト表示を壊さないため。
- [x] `URLでリンク` 入力欄で文字を入れたとき、ローカル台帳の詳細URL・物件名・住所と照合した候補を表示する。意図: 手入力の負担を減らし、読み込み済み掲載への紐づけを素早く行えるようにするため。
- [x] ヘッダのフィルター状態は `HOME'S` と `SUUMO` で別 key に保存する。意図: サイトごとに違う絞り込みを使っても、相互に上書きしないようにするため。
- [x] manual link group で同一サイト内に重複した掲載は、先頭 1 件だけ残して 2 件目以降を非表示にする。意図: 同じサイト内で束ねた重複掲載を一覧上でも整理して見やすくするため。
- [x] `紐づけ一覧` の各行に `リンク` / `解除` ボタンを置き、その行だけ即時に更新されるようにする。意図: どの相手に何が起きる操作かを UI 上で直感的に伝えるため。
- [x] fingerprint が一致しない掲載でも、保存済みの詳細URLを手入力すれば manual link できる入口を panel に追加する。意図: 物件名や住所表記がずれていても、ユーザー判断で重複掲載を束ねられるようにするため。
- [x] 紐づけ一覧を縦積みではなく、物件名リンク・家賃・住所が読みやすい要約表示へ整える。意図: 候補一覧が縦長になりすぎず、比較と確認を素早くできるようにするため。
- [x] 紐づき件数と一覧は、自分自身ではなく他掲載とのリンクだけを対象に表示する。意図: `紐づき` の意味を直感と揃え、解除可否の誤解を減らすため。
- [x] 保存の正本を `chrome.storage.local` へ切り替え、状態・掲載台帳・link group を分けて保持する。意図: 重複候補の判定と再紐づけに必要な掲載データをローカルで継続保持するため。
- [x] `物件名 + 住所 + 家賃` の fingerprint と manual link group で、HOME'S / SUUMO 横断のメモ共有を解決する。意図: 自動候補と明示的な解除・再リンクを両立するため。
- [x] panel に `紐づき N件` と `紐づけ一覧` を追加し、行ボタンベースの再紐づけ UI を載せる。意図: 重複件数と候補一覧を視覚的に把握し、その場でリンク管理できるようにするため。
- [x] JSON schema を `schemaVersion: 2` へ更新し、状態だけでなく掲載台帳と link 情報も書き出し・復元できるようにする。意図: ローカルで育つリンク構造をバックアップで失わないようにするため。
- [x] 旧 sync/local の状態を初回だけ local 正本へ移行する。意図: 既存メモを捨てずに local-only 実装へ切り替えるため。
- [x] link 管理用の smoke と sample fixture 確認を追加する。意図: fingerprint 抽出や JSON v2 の主要フックを依存なしで再確認できるようにするため。
- [x] 拡張名とツールバー名を `賃貸物件 条件一覧アシスタント` に変更する。意図: HOME'S 専用ではなく、SUUMO を含む賃貸一覧アシスタントとして表示名を実態に合わせるため。
- [x] `content.js` をサイト別設定で分岐する構造へ整理し、HOME'S `condition-list` / `condition1` / SUUMO `FR301FC001` を同じ保存・フィルタ基盤で扱えるようにする。意図: 今回の SUUMO 追加を最小差分で入れつつ、今後の対象追加も進めやすくするため。
- [x] SUUMO 一覧の room ごとにコメント UI と保存 ID を付与し、建物内の全 room が filter 対象外のときだけ建物 `<li>` を非表示にする。意図: HOME'S `condition1` と同じ建物粒度の絞り込み体験を SUUMO でも揃えるため。
- [x] SUUMO のページャから `次へ` をたどり、後続ページの建物ブロックを `Page N` 区切り付きで現在ページへ連結する。意図: 複数ページに分かれた一覧を 1 画面で連続して扱えるようにするため。
- [x] JSON 書き出し名、manifest、smoke、storage doc を SUUMO 対応後の名称と仕様へ更新する。意図: 実装と再利用可能な検証・ドキュメントの内容を一致させるため。
- [x] 実装前後の smoke と差分確認を行う。意図: 既存 HOME'S 挙動を壊さずに SUUMO 対応を追加できたことを確認するため。
- [x] JSON 書き出しファイル名に `YYMMDD-HHMMSS` サフィックスを付け、基準を最終更新日時にする。意図: バックアップを作った時刻ではなく、中身がいつ更新された状態かをすぐ判断できるようにするため。
- [x] 最小の smoke と差分確認を行う。意図: JSON 書き出し名変更で既存の export/import フックを壊していないことを確認するため。
- [x] ヘッダに `最終更新日時` を表示する。意図: いま見ているメモ情報がいつ更新されたものかを一目で判断できるようにするため。
- [x] ヘッダから保存状態表示を外し、最終更新日時だけを残す。意図: local-only 化後は保存先の状態表示より、更新時刻だけの方が情報として素直なため。
- [x] 最小の smoke と差分確認を行う。意図: ヘッダ表示追加で既存のメモ保存・フィルタ UI を壊していないことを確認するため。
- [x] `condition1` の PR 枠 `.kksearch.rentListPrDesign` を非表示にする。意図: 通常の物件一覧だけを見られるようにし、PR 枠を視界から外すため。
- [x] 最小の smoke と差分確認を行う。意図: PR 枠の非表示追加で既存の content script 構成を壊していないことを確認するため。
- [x] ステータス番号を再採番し、`除外候補` を `8`、`要確認/検討中/本命` を `1/2/3` に揃える。意図: UI 表示、フィルタ、保存値を新しい番号体系へ統一するため。
- [x] JSON 書き出し / 読み込みと保存済み値の正規化を新しい番号体系へ揃える。意図: 旧値 `1/2/3/4` との互換処理を持たず、新形式だけを扱うため。
- [x] 最小の smoke と差分確認を行う。意図: ステータス値変更で既存の保存・表示フックを壊していないことを確認するため。
- [x] ヘッダのフィルターチェック状態を `chrome.storage.local` に保存し、同じブラウザで再訪したときに復元する。意図: 毎回チェックを入れ直さず前回の絞り込み条件をそのまま使えるようにするため。
- [ ] ヘッダの初期描画が保存済み状態を反映することを確認する。意図: 復元前提の状態と UI 表示がずれないようにするため。
- [x] 最小の smoke と差分確認を行う。意図: 保存フック追加で既存の JSON / ステータス機能を壊していないことを確認するため。
- [x] 紐づき共通コメントとは別に、各掲載へ短い個別コメント `itemComment` を持てるようにする。意図: 同じ物件グループでも掲載ごとの差分メモを残せるようにするため。
- [x] 表示中の掲載ではステータス右側、紐づけ一覧では badge 群右側に個別コメント表示と `編集` ボタンを追加する。意図: 一覧比較の視線移動を増やさず、その場で短文メモを編集できるようにするため。
- [x] 個別コメント編集を 1 行入力モーダルへ統一する。意図: textarea を増やさず、簡単メモの編集体験を揃えるため。
- [x] `condition1` のページャから「次ヘ」を解決し、後続ページ URL を順にたどれるようにする。意図: 複数ページに分かれた一覧を 1 画面で連続して扱えるようにするため。
- [x] `.prg-bundle` の既存構造を保ったまま、bottom action の手前へ後続ページの building ブロックだけを連結する。意図: 既存レイアウトや一括操作 UI を壊さずに物件行だけ増やすため。
- [x] 連結後の building / room に既存の `scan()` と filter 処理が効く状態を確認する。意図: 追加分でもステータス表示と絞り込みが同じように動くようにするため。
- [x] 最小の smoke check と差分確認を行う。意図: 構文エラーや主要フックの欠落を防ぐため。
- [x] 後続ページを連結するときに `Page N` の区切りを挟み、どのページ由来の物件か分かるようにする。意図: 追加分の視認性を上げてスクロール時の認知負荷を下げるため。
- [x] PowerShell なしでも回せる cross-platform の smoke 入口を追加する。意図: macOS / Linux 環境でも同じ最小検証を再利用できるようにするため。
- [x] smoke の正本と docs 参照を Node 版へ寄せる。意図: 実行環境差で検証経路が分岐しすぎるのを防ぐため。
- [x] `condition-list` のページャから「次ヘ」を解決し、後続ページ URL を順にたどれるようにする。意図: building 一覧でも複数ページを 1 画面で続けて扱えるようにするため。
- [x] `.bundle` の既存構造を保ったまま、bottom action の手前へ後続ページの building ブロックと `Page N` 区切りを連結する。意図: 一括操作 UI を壊さずにページ境界だけ分かるようにするため。
- [x] `condition-list` 追加分にも既存の `scan()` と filter 処理が効くことを確認する。意図: 連結後もメモ UI と絞り込みが同じ動きになるようにするため。
- [x] 紐づけ一覧の `SUUMO` などの右側に、保存済みステータスを色付き badge で表示する。意図: リンク先の検討状況を一覧比較の中で即座に判断できるようにするため。
- [x] 紐づけ一覧でステータスの右側にコメント要約を 25 文字程度で表示し、全文はツールチップで確認できるようにする。意図: 一覧比較の視線移動を増やさず、必要なときだけ詳細コメントを読めるようにするため。
- [x] URL サジェストにもステータスとコメント要約を表示し、紐づけ一覧と共通ヘルパーで描画する。意図: 候補選択の前段でも同じ判断材料を見られるようにし、表示ロジックの分岐を減らすため。
- [x] `紐づけ一覧` の下に `もしかして` を追加し、現掲載または既存リンク名と一致し、住所の先頭数字までが一致する未リンク掲載を出す。意図: fingerprint では拾えない表記揺れ候補を、その場で追加リンクしやすくするため。

## レビュー
- [ ] `HOME'S condition1` 実ページで、画像・条件列・チェック列の横詰まりが軽減され、横スクロールや崩れが出ないことを手動確認する
- [ ] manual link で comment を持つ 2 掲載を束ねたとき、shared comment が旧→新の順で連結され、同文は重複しないことを手動確認する
- [x] grouped UI 着手前に `node ./tests/smoke.js` を実行し、既存 smoke の基準を確認する
- [x] grouped UI 変更後に `node ./tests/smoke.js` を実行する
- [ ] 手動で HOME'S / SUUMO 実ページの `紐づき中` / `リンク` / `もしかして` が group 単位で開閉し、shared state/comment がヘッダに残ることを確認する
- [ ] 手動で `URLでリンク` 入力中のサジェストも group 単位で開閉し、メンバー行から `リンク` できることを確認する
- [ ] 手動で `候補一覧` / `もしかして` / `URLサジェスト` の `リンク` と `紐づき中` の `解除` が group ヘッダ右側にだけ出て、group 全体へ効くことを確認する
- [ ] 手動で `紐づき中` の `解除` だけは各メンバー行に残り、1 件ずつ外せることを確認する
- [ ] 手動で `紐づき中` の group ヘッダ `解除` が、その group 全体を一度に外せることを確認する
- [x] `athome` 対応追加前に `node ./tests/smoke.js` を実行し、既存 smoke の基準を確認する
- [x] `athome` 用 assertion を追加した smoke を一度 fail させる
- [x] `athome` 対応追加後の `node ./tests/smoke.js` を実行する
- [ ] 手動で `athome` 実ページの表示、保存、filter、`紐づけ一覧`、pagination 連結を確認する
- [x] snapshot の `homes` / `suumo` で `紐づけ一覧` と `もしかして` の行幅が揃うことを目視確認する
- [ ] URL入力中に候補が表示され、候補クリックでリンク操作につなげられることを確認する
- [x] local-only / link 管理追加後の `node ./tests/smoke.js` を実行する
- [ ] 手動で HOME'S / SUUMO 実ページの紐づき件数、解除、再リンクを確認する
- [x] SUUMO 対応追加後の `node ./tests/smoke.js` を実行する
- [ ] 手動で HOME'S / SUUMO 実ページの表示と filter を確認する
- [x] 構文確認を実行する
- [x] 差分を確認して不要な変更がないことを確認する
- [x] 紐づけ一覧ステータス表示追加後の `node ./tests/smoke.js` を実行する
- [x] コメント要約表示追加後の `node ./tests/smoke.js` を実行する
- [x] サジェストへの共通表示追加後の `node ./tests/smoke.js` を実行する
- [ ] `もしかして` に、例のような `品川区3-4-2` と `品川区3` は出て `品川区2` は出ないことを手動確認する
- [ ] 個別コメントが、表示中の掲載と紐づけ一覧の両方で表示・編集できることを手動確認する

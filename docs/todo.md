# TODO

## 今回の変更
- [x] 保存先を `chrome.storage.sync` 中心に切り替える。理由: Chrome ログイン済みブラウザ間で同じ非表示・コメント情報を使い回せるようにするため。
- [x] 旧 `chrome.storage.local` データを移行する。理由: 既存ユーザーのメモと非表示設定を消さずに移行するため。
- [x] コメント保存を debounce する。理由: 同期ストレージの書き込み制限に引っかかりにくくするため。
- [x] 他ブラウザや他タブでの更新を画面へ反映する。理由: 同期後の体験を「再読み込み前提」にしないため。

## レビュー
- [x] 差分が保存層と同期反映に閉じており、UI 仕様の変更を最小限に抑えていることを確認した。
- [x] 最小検証として構文チェックと git diff 確認を実施する予定。

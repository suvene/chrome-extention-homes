# TODO

## 今回の作業
- [x] `condition-list` と `condition1` で一致しうる識別子を整理する。意図: 保存キーの粒度差でステータスが途切れないようにするため。
- [x] room 単位の保存キーを優先し、物件単位キーを fallback にする。意図: 同じ部屋は同じ状態を引き継ぎつつ、部屋キーが取れないケースも救うため。
- [x] `condition1` の行グループごとにパネル表示とフィルタ適用ができるようにする。意図: 物件内の各部屋で個別に状態を見える化するため。
- [x] 既存の smoke check を更新し、今回の識別子ロジックが落ちていないことを確認できるようにする。意図: 将来の修正で `condition1` 対応が消えるのを防ぐため。
- [x] 最小の検証を行い、差分とリスクを見直す。意図: 既存の `condition-list` 側を壊していないことを確認するため。

## レビュー
- [x] 構文確認を実行する
- [x] 差分を確認して不要な変更がないことを確認する

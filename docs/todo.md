# TODO

## 今回の作業
- [x] `condition1` の room フィルター結果から building 単位の可視状態を判定する。意図: 全室が非表示の building を一覧から外せるようにするため。
- [x] building 全体を閉じる専用 class と判定処理を追加する。意図: room 行だけ消えて空の building が残る状態を防ぐため。
- [x] smoke check を更新し、building 可視制御のフックが消えていないことを確認できるようにする。意図: 将来の修正でこの挙動が抜け落ちるのを防ぐため。
- [x] 最小の検証を行い、差分とリスクを見直す。意図: `condition1` だけでなく既存一覧側の表示制御も壊していないことを確認するため。

## レビュー
- [x] 構文確認を実行する
- [x] 差分を確認して不要な変更がないことを確認する

# TODO

## 今回の作業
- [x] `chrome.storage.sync` の現行保存構造を確認する。意図: 既存同期モデルを崩さずに import/export を足すため。
- [x] JSON書き出しで保存済みステータス全件を出力できるようにする。意図: 同期不良時にバックアップを取れるようにするため。
- [x] JSON読み込みで書き出し内容を取り込めるようにする。意図: 他端末やバックアップから状態を復元できるようにするため。
- [x] 最小の検証を行い、差分とリスクを見直す。意図: 既存の一覧操作を壊していないことを確認するため。
- [x] 今回使った再利用可能な確認を `tests/` に置く。意図: 同じ確認を毎回手で打ち直さずに済むようにするため。
- [x] `tests/` の実行導線を関連ドキュメントへ明記する。意図: 今後の修正時に必要な確認を迷わず実行できるようにするため。

## レビュー
- [x] 構文確認を実行する
- [x] 差分を確認して不要な変更がないことを確認する
